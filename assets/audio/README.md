# Easter egg audio

Primary file: `Ahoy.mp3` (also accepts `easter_egg.mp3` / `easter_egg.wav` as fallbacks).

Triggered by a **long-press** on the bottom-right `/` keypad key (tap inserts the items/stops separator).
Keep it short (~1-2s). Registered in pubspec.yaml under flutter: assets:.
